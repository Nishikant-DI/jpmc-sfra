<isloop items="${pdict.order.billing.payment.applicablePaymentMethods}" var="paymentOption">
    <isif condition="${paymentOption.ID === 'CREDIT_CARD'}">
        <isset name="jpmcGetKeyUrl" value="${dw.system.Site.current.getCustomPreferenceValue('JPMCGetKeyUrl') || ''}" scope="page" />
        <isset name="jpmcEncryptionUrl" value="${dw.system.Site.current.getCustomPreferenceValue('JPMCEncryptionUrl') || ''}" scope="page" />
        <isif condition="${jpmcGetKeyUrl}">
            <script src="${jpmcGetKeyUrl}"></script>
        </isif>
        <isif condition="${jpmcEncryptionUrl}">
            <script src="${jpmcEncryptionUrl}"></script>
        </isif>
        <input type="hidden" name="csrf_token" value="<isprint value="${dw.web.CSRFProtection.generateToken()}" encoding="htmldoublequote"/>"/>
        <isinclude template="checkout/billing/paymentOptions/creditCardContent" />
    <iselseif condition="${paymentOption.ID === 'DW_APPLE_PAY'}"/>
        <isinclude template="checkout/billing/paymentOptions/applePayContent" />
    </isif>
    <isif condition="${paymentOption.ID === 'JPMC_GOOGLE_PAY'}">
        <isinclude template="checkout/billing/paymentOptions/googlePayContent" />
    </isif>
</isloop>
